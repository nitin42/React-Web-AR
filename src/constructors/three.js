const THREE = require('three')

/**
 * Convenient (fight me!)
 */
const WebGl = THREE.WebGLRenderer
const Color = THREE.Color
const Scene = THREE.Scene
const Camera = THREE.Camera

export {
  WebGl,
  Color,
  Scene,
  Camera
}