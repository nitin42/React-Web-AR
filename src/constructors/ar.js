/**
 * ARToolKit globals
 */

const ReactArToolKitContext = window.THREEx.ArToolkitContext

const ReactArToolKitMarkerController = window.THREEx.ArMarkerControls

const ReactArToolKitSource = window.THREEx.ArToolkitSource

export { ReactArToolKitContext, ReactArToolKitMarkerController, ReactArToolKitSource }
